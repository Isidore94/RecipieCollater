# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# uv / virtualenvs
.venv/
uv.lock.bak

# Runtime data — never committed (contains the family's recipes, tokens, backups)
/data/

# Secrets — never committed
.env
/deploy/env
*.local.env

# OS / editor
.DS_Store
*.swp
