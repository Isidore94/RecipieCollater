# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# uv / virtualenvs
.venv/
uv.lock.bak

# PyInstaller build output (the .exe is ~150 MB; never committed)
/build/
/dist/

# Runtime log written by the desktop app when it has no console
*.log

# Runtime data — never committed (contains the family's recipes, tokens, backups)
/data/

# Secrets — never committed
.env
/deploy/env
*.local.env

# OS / editor
.DS_Store
*.swp
